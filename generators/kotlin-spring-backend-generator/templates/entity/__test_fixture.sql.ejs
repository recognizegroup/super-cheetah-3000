<% const testDataEntity = testData.fetchTestDataForEntity(entity, 1) %>
-- Create sample data
INSERT INTO <%= toSnakeCase(entity.name) %> (
    id,<% entity.fields.forEach((field, index) => { %>
    <%= toSnakeCase(field.name) %>,<% }) %>
    created_at,
    updated_at
)
VALUES (
    1,<% entity.fields.forEach((field, index) => { %>
    '<%= testDataEntity[field.name] %>',<% }) %>
    CURRENT_TIMESTAMP,
    CURRENT_TIMESTAMP
);


SELECT setval('public.<%= toSnakeCase(entity.name) %>_seq', 100);
