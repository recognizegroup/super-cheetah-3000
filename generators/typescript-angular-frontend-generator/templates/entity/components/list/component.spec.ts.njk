---
id: test
dependencies: [list-component]
---
import { ComponentFixture, TestBed } from '@angular/core/testing';

import { {{ dependencies.list-component.name }} } from './{{ entity.name | plural | kebabCase }}.component';

describe('{{ dependencies.list-component.name }}', () => {
  let component: {{ dependencies.list-component.name }};
  let fixture: ComponentFixture<{{ dependencies.list-component.name }}>;

  beforeEach(() => {
    TestBed.configureTestingModule({
      declarations: [{{ dependencies.list-component.name }}]
    });
    fixture = TestBed.createComponent({{ dependencies.list-component.name }});
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
