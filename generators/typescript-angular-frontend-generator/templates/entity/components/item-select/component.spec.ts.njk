---
id: test
dependencies: [item-select-component]
---
import { ComponentFixture, TestBed } from '@angular/core/testing';

import { {{ dependencies.item-select-component.name }} } from './{{ entity.name | plural | kebabCase }}.component';

describe('{{ dependencies.item-select-component.name }}', () => {
  let component: {{ dependencies.item-select-component.name }};
  let fixture: ComponentFixture<{{ dependencies.item-select-component.name }}>;

  beforeEach(() => {
    TestBed.configureTestingModule({
      declarations: [{{ dependencies.item-select-component.name }}]
    });
    fixture = TestBed.createComponent({{ dependencies.item-select-component.name }});
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
